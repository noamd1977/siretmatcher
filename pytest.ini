[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
python_files = test_*.py
markers =
    integration: tests nécessitant la base de données réelle
    regression: tests de non-régression du pipeline
    slow: tests lents (appels API externes)
    performance: tests de performance (temps de réponse)
