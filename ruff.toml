target-version = "py312"
line-length = 120

[lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM"]
ignore = ["E501"]  # Longueur de ligne gérée manuellement pour l'instant

[lint.isort]
known-first-party = ["siret_matcher"]
